import { Label } from "@/elements/label/Label";

const Profile = () => {
    return (
        <>
            <Label text="아이의 나이대가 어떻게 되나요?" css="profileLabel" />
        </>
    );
};

export default Profile;
